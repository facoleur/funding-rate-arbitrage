networks:
  proxy:
    external: true

services:
  postgres:
    environment:
      POSTGRES_USER: option_arb
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: option_arb

  migrate:
    image: ghcr.io/${GHCR_OWNER}/option-arb:${RELEASE_TAG}
    environment:
      DATABASE_URL: postgresql+asyncpg://option_arb:${POSTGRES_PASSWORD}@postgres:5432/option_arb
      ALEMBIC_DATABASE_URL: postgresql://option_arb:${POSTGRES_PASSWORD}@postgres:5432/option_arb

  api:
    image: ghcr.io/${GHCR_OWNER}/option-arb:${RELEASE_TAG}
    environment:
      DATABASE_URL: postgresql+asyncpg://option_arb:${POSTGRES_PASSWORD}@postgres:5432/option_arb
    ports: []
    networks:
      default:
      proxy:
        aliases:
          - option-arb-api

  workers:
    image: ghcr.io/${GHCR_OWNER}/option-arb:${RELEASE_TAG}
    environment:
      DATABASE_URL: postgresql+asyncpg://option_arb:${POSTGRES_PASSWORD}@postgres:5432/option_arb

  executor:
    image: ghcr.io/${GHCR_OWNER}/option-arb:${RELEASE_TAG}
    environment:
      DATABASE_URL: postgresql+asyncpg://option_arb:${POSTGRES_PASSWORD}@postgres:5432/option_arb

  frontend:
    image: nginx:alpine
    volumes:
      - /srv/option-arb/frontend/dist:/usr/share/nginx/html:ro
    networks:
      default:
      proxy:
        aliases:
          - option-arb-frontend
    restart: unless-stopped
